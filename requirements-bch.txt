-r requirements.txt

# Experimental BCH cross-check environment.  PennyLane 0.45 requires NumPy
# >=2, while the chemistry stack in this repository is pinned to NumPy 1.26.
# Version 0.44.1 contains the same trotter_error API used here and has a clean
# dependency resolution with the project pin.
pennylane==0.44.1
pennylane-lightning==0.44.0
jax==0.4.35
jaxlib==0.4.35
ml-dtypes==0.4.1
